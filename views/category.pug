extends layout

block content
  h1: a(href=categoryUrl) #{categoryName}
  p.lead Great news!
  hr
  .row
  each item in result
    .col-sm-6
      h2= item.rssname
      p= item.trimRssurl
      i= item.timeago
      .row
        .col-sm-1
        .text-right
          a.btn.btn-default(href='/i/' + item.categoryid + '/' + item.id, role='button') View details »